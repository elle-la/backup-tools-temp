# Created imperatively with:
#   kubectl create secret generic backup-passphrase \
#     --from-literal=passphrase='correct-horse-battery-staple'
#
# Declarative version kept here for reference / repeatability.
# (In real usage you would never commit a real secret value to a file
# like this - use `kubectl create secret` directly or a secrets manager.)
apiVersion: v1
kind: Secret
metadata:
  name: backup-passphrase
type: Opaque
stringData:
  passphrase: correct-horse-battery-staple
